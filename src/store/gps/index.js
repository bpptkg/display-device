import baseline from './baseline'
import coordinate from './coordinate'
import graphs from './graphs'

export default {
  namespaced: true,
  modules: {
    namespaced: true,
    baseline,
    coordinate,
    graphs,
  },
}
